# Changelog

## Unreleased

- 为信仰命途建立增量索引，命途查询不再遍历全部信仰定义。
- 收敛源码注释，仅保留公共契约、幂等和并发边界说明。
- 将固定与动态信仰祷词统一登记到 Core，并提供带唯一性约束的 O(1) `resolvePrayerWord()` 查询。
- 动态信仰创建、祷词更新与注销共用注册表锁，避免并发写入造成数据库和内存索引不一致。
- 动态信仰创建从零信徒开始，并增加仅允许无信徒信仰使用的补偿注销接口。
- 修复业务模块重载时同 owner 相同物品定义不能幂等注册的问题。
- 增加 Core Gameplay SDK、单一配置声明、统一响应构造器和玩家错误类型。
- 原子事务增加带加成的 `tx.economy.reward()`，玩法不再需要在事务外预览后固定入账。
- 增加简化玩法的原子状态、自动审计来源和跨平台事件幂等支持。

## 3.0.0-alpha.2

- 完善身份绑定约束、批量操作、背包查询和受限原子事务。
- 修复 SQLite 嵌套事务、业务表注册时机及连续写入快照冲突。
- 优化用户、物品、经济和事务热路径，并重整内部服务目录。
- 增加运行版本导出，公开名称调整为 CoCoFaith Core。

## 3.0.0-alpha.1

- 建立 UID、身份、用户数据、背包和通用业务数据表。
- 加入经济、物品、职业、信仰、权限、效果、Hook 与生命周期服务。
- 提供受限 Business Scope、原子事务、幂等流水和批量操作。
- 修复 SQLite 嵌套事务与并发根事务冲突。
- 加入身份解析缓存和配置 reload。
